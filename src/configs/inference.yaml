defaults:
  - model: la
  - metrics: la
  - datasets: la
  - dataloader: la
  - transforms: la
  - _self_
inferencer:
  device_tensors: ["data_object", "labels"]
  device: auto
  save_path: "la"
  seed: 1
  from_pretrained: "saved/model_best.pth"
